import '../styles/globals.css';

/**
 * Custom App component to load global styles and render pages.
 */
export default function MyApp({ Component, pageProps }) {
  return <Component {...pageProps} />;
}
